from typing import List

from Entity.Task import Task


def sort_tasks(lst: List[Task]) -> List[Task]:
    """
    Sorts tasks by due date or something, puts completed tasks at the bottom.
    Returns the list of tasks
    note: you can use sorted() along with a method that determines the order btw.
    """
    pass
